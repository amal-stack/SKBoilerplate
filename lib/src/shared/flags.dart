/// The API is currently having issues with image and invoice uploads.
/// Since grade calculation does not depend on these uploads, this flag when
/// set to true will bypass all upload steps and directly proceed to grade calculation.
/// 
/// Set to false to enable uploads.
const disableUploads = true;
